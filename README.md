# customer-churn-prediction-with-sql
Customer churn prediction for a multi‑category e‑commerce platform using SQL. Synthetic dataset (100k customers, 20k orders) generated and loaded into SQL. Feature engineering pipeline in progress: intervals, return ratios, cancellation patterns, item‑level behavior
